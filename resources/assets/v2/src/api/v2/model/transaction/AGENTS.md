# AGENTS.md

This directory is covered by the nearest parent `AGENTS.md` in the repository tree.
Follow the closest ancestor instruction file before making edits in this directory.

- Do not introduce speculative assumptions about file-level behavior.
- If logic is duplicated, prefer centralization in the nearest shared module layer.
- Handle errors with explicit, graceful catch handling.
- Use correct API status codes and structured errors for REST changes.
- Keep changes scoped and lightweight for this directory.