# AGENTS.md

This directory is covered by the nearest parent `AGENTS.md` in the repository tree.
Follow the closest ancestor instruction file before making edits in this directory.

- Apply the nearest module AGENTS instructions first.
- Use explicit error handling and shared reusable patterns for backend/frontend edits.
- Keep API changes aligned with REST status-code conventions.
- Do not treat generated/test artifacts as canonical implementation targets.